using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

/// <summary>
/// Пространство имен должно совпадать с настройкой проекта Default Namespace.
/// Если это не так могут возникнуть проблемы с поиском внедренных в сборку ресурсов.
/// </summary>
namespace TSB.Lib
{
    /*
    internal class Settings
    {
        public static string getAssemblyNamespace()
        {
            return typeof(Settings).GetType().Namespace;
        }

    }
    */

}
